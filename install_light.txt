install Archlinux: 
	wifi connect:
- iwctl station wlan0 connect _ORBITA_

run script tu install:
- curl -fsSL christitus.com/linux | sh
	System Setup
	Arch linux
	Arch server setup
- reboot

## install yay:
## run script tu install:
	- curl -fsSL christitus.com/linux | sh

## install pakeges:
	sudo pacmna -S ly xorg xorg-xinit micro mousepad bspwm sxhkd thunar alacritty 
	polybar git lxappearance unzip xarchiver 

## add autostart:
	cp .xinitrc /home/w22
	chmod +x .xinitrc

## create bspwm folder and files:
	- mkdir -p .config/bspwm
	- mkdir -p .config/sxhkd
	- cp /home/w22/.config/sxhkd/sxhkdrc
	- cp /home/w22/.config/bspwm/bspwmrc
	- chmod +x /home/w22/.config/sxhkd/sxhkdrc
	- chmod +x /home/w22/.config/bspwm/bspwmrc

## alacritty:
	- mkdir -p .config/alacritty
	
## install fonts:
sudo pacman -S nerd-fonts-git 

## add language:
	- sudo micro /etc/X11/xorg.conf.d/00-keyboard.conf
		Section "InputClass"
			Identifier "system-keyboard"
			MatchIsKeyboard "on"
			Option "XkbLayout" "us,ua"
			Option "XkbOptions" "grp:alt_shift_toggle"
		EndSection

## install auto-mount-usb:
	- sudo pacman -S udisks2 gvfs gvfs-mtp gvfs-gphoto2 gvfs-afc polkit polkit-gnome


instal GUEST vbox:
- sudo pacman -S virtualbox-guest-utils
- sudo systemctl enable --now vboxservice.service

add to boot ly:
- systemctl enable ly.service

install menu:
- sudo pacman -S menumaker unzip xarchiver


install soft:
- sudo yay -S google-chrome

add language:
- sudo nano /etc/X11/xorg.conf.d/00-keyboard.conf
	Section "InputClass"
		Identifier "system-keyboard"
		MatchIsKeyboard "on"
		Option "XkbLayout" "us,ua"
		Option "XkbOptions" "grp:alt_shift_toggle"
	EndSection
- reboot




